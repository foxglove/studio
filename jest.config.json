{
  "//": "Our top level jest config to reference all the projects under our monorepo. We exclude the integration test since it involves performing a webpack build and doesn't support 'watch' or 'debug' in the same way",
  "projects": [
    "<rootDir>/app/jest.config.ts",
    "<rootDir>/ci/jest.config.ts",
    "<rootDir>/packages/*/jest.config.ts"
  ]
}
