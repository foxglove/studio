{
  "extends": "@foxglove/tsconfig/base",
  "include": ["./**/*"],
  "compilerOptions": {
    "composite": true,
    "incremental": true,
    "rootDir": "./src",
    "outDir": "./dist",
    "noEmit": true, // Don't need to emit anything since this is a types-only package
    "lib": ["es2020"]
  }
}
