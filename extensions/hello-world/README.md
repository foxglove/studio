# hello-world

A simple Foxglove Studio extension that can be used as a reference when creating your own
extension. Don't copy the files in this extension to create your own. Instead, run
`npx fox create <name>`.
